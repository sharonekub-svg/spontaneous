# Pins Fastlane so CI and your Mac use the same version (reproducible builds).
# Install locally with:  bundle install
# Run lanes with:        bundle exec fastlane beta
source "https://rubygems.org"

gem "fastlane"
gem "cocoapods"
